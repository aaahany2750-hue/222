export const generateAIReply = async (message, tone = 'professional') => {
  // Placeholder for external API integration (OpenAI/Anthropic/etc.)
  return `(${tone} AI) I received: "${message}". This is where your model response appears.`;
};
